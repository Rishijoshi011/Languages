LXI H,0000H 	; HL AS A ACCUMULATOR 
LXI D,0000H 	; DE AS A SECOND OPERAND

LDA 2000H	; LOADING ACCUMLATOR
ADI 00H		; CHECKING IF OPERAND IS 0
JZ STORE	

MOV E,A		; MOVING FIRST OPERAND AKA MULTIPLIER

LDA 2001H	; LOADING SECOND OPERAND
ADI 00H		; CHECKING IF OPERAND IS 0
JZ STORE

MOV C,A		; MOVING SECOND OPERAND AKA MULTIPLICANT. C ALSO USED AS COUNT
BACK:DAD D

DCR C
JNZ BACK

STORE:SHLD 2002H ; STORING RESULTS 

; 2002 HIGHER BITS MEANS FIRST TWO DIGITS
; 2003 LOWER BITS MEANS SECOND TWO DIGITS

HLT