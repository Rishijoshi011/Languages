#include <stdio.h>
  int main ()
{
    int m,b,r;
     printf("\n\t Enter two values");
     scanf("%d%d",&m,&b);
    	if(m>b)
    {
    	 printf("%d is bigger then %d",m,b);
	}
		else 
	{
		 printf("\n\t%d is biggar then %d",b,m);
	}
}
  
