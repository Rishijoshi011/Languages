#include <stdio.h>
     int main()
{
	 int m,b,r;
	   printf("\n\t Enter two values");
	   scanf("%d%d",&m,&b);
	  if(m==b)
	{
		printf("\n\t %d is equeal to %d",m,b);
	} 
	  else 
	{
		printf("\n\t %d isn't equal to %d",m,b);
	}  
}
